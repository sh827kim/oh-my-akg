auto-install-peers=true
shamefully-hoist=false

# PostCSS/Tailwind v4가 monorepo 어느 레벨에서든 resolve되도록 루트에 hoist
# shamefully-hoist=false를 유지하면서 필요한 패키지만 선택적으로 호이스트
public-hoist-pattern[]=tailwindcss
public-hoist-pattern[]=@tailwindcss/*
